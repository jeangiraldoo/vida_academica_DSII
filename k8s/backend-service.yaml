apiVersion: v1
kind: Service
metadata:
  name: vida-backend
  labels:
    app: vida-backend
spec:
  type: ClusterIP
  selector:
    app: vida-backend
    track: blue
  ports:
    - name: http
      port: 80
      targetPort: 8000
